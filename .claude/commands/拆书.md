---
description: '导入教材并按章节拆书，生成全书大纲与分块笔记'
argument-hint: '[教材文件路径]'
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
---

帮我导入一本教材并按章节拆分，让 AI 能一章一章地读完。

## 步骤

1. 确认教材来源：如果我在对话里附了文件路径或 @了文件就用它；否则问我要路径。
   常见格式：PDF、EPUB、Word (.docx)。
2. 选择拆书方式：
   - 普通电子版教材（有文字层）：用文字提取，命令：
     优先用 `_工具/拆书.bat`（把教材拖进窗口）；命令行方式用
     `_工具/.venv/Scripts/python.exe _工具/split_textbook.py "<路径>" --out "04-教材分块" --split-mode chapter`
     （没有 .venv 时用系统 `python` 代替）
   - 扫描版（提示"提取不到文字"）：先用 `_工具/拆书-OCR.bat`（本地免费 OCR）；
     数学书/公式多则用 `_工具/拆书-MinerU.bat`（云端，公式转 LaTeX，
     需先运行 `_工具/设置MinerU令牌.bat` 保存 Token）。
   - 也可以直接告诉我，让用户双击对应的 .bat 并拖入文件。
3. 拆分成功后：
   - 在 `04-教材分块/<书名>/` 下应生成：`00-教材信息.md`、`00-目录.md`、
     `01-全书大纲.md`、若干 `章节XXX-*.md` 分块。
   - 用中文汇报：书名、识别到的章节数、分块数、书文件夹路径，并贴出
     [[01-全书大纲]] 的章节清单（只列章节名，不贴正文）。
4. 询问用户下一步：是否基于这本书创建学习主题（运行 /新主题-拆解与计划），
   还是先从第一章开始逐章学习。

## 注意事项

- 不要一次把整本书的内容读进对话；分块笔记就是给 AI 逐块阅读用的。
- 如果书已经拆过（书文件夹已存在），不要重复新建文件夹，原地更新即可，
  旧分块会自动备份到 `_备份/`。
- 中文回复，解释要通俗。
